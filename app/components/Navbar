export default function Navbar() {
  return (
    <header
      style={{
        background: "#111827",
        padding: "20px",
        color: "white",
        marginLeft: "260px",
      }}
    >
      <h2>🚀 AI Operating System</h2>
    </header>
  );
}
